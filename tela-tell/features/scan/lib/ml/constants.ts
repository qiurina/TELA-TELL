import { SUPPORTED_FABRICS } from '@/data/fabrics/fabrics';

/** Must stay in sync with ml-training/labels.py FABRIC_CLASSES. */
export const MODEL_LABELS = SUPPORTED_FABRICS;

/** Must stay in sync with ml-training/labels.py IMAGE_SIZE. */
export const IMAGE_SIZE = 224;